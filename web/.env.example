# Base URL of the backend API used by the frontend.
# Copy this file to `.env` for local development and adjust as needed.
VITE_API_URL=http://localhost:6010

# Grafana Faro collector endpoint (see telemetry/compose.yml).
# Leave unset to disable frontend telemetry.
VITE_FARO_URL=http://localhost:12347/collect
